#include "etl.h"

int convert(const legacy_map *input, const size_t input_len,
            new_map **output)
{
    int output_len = 0;
    (void)input;
    (void)input_len;
    (void)output;
    return output_len;
}