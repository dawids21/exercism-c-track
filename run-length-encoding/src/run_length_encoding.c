#include "run_length_encoding.h"
#include <stdlib.h>

char *encode(const char *text)
{
    char *encoded = NULL;
    (void)text;
    return encoded;
}

char *decode(const char *data)
{
    char *decoded = NULL;
    (void)data;
    return decoded;
}