#include "rail_fence_cipher.h"

static const int MAX_RAIL_LENGTH = 30;

char *encode(char *text, size_t rails)
{
    char *output = NULL;
    (void)text;
    (void)rails;
    (void)MAX_RAIL_LENGTH;
    return output;
}

char *decode(char *ciphertext, size_t rails)
{
    (void)ciphertext;
    (void)rails;
    return NULL;
}